name: Trigger Workflow in Repository deepak

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Repository B Workflow
        run: |
          curl -X POST -H "Authorization: token ${{ secrets.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 }}" \
            -H "Accept: application/vnd.github.everest-preview+json" \
            https://github.com/deepakcis/Cron_testing/dispatches \
            -d '{"event_type": "trigger_cron.yml"}'


